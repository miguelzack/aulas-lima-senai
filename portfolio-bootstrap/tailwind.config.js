/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './index.html', 
    './src/**/*.{html,js}'
  ],
  theme: {
    extend: {
      // É aqui que você colocará suas "variáveis"
      // (cores, fontes, etc.) no futuro
    },
  },
  plugins: [],
}